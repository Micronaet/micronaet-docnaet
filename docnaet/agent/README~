Agent for open in modify state the images from form fashion
In Windows only:

1. reg files active fashion:// type of link
2. python files open document and is esecuted from fashion://link
3. Copy folder in c:\ (as fashion)
4. Modify image with path correct (folder image)
5. Modify bat with call to python


Note:
for convert all image with lower case use:
for i in *.jpg; do mv "$i" $(echo "$i" | awk -F '.jpg' '{ print tolower($1) FS }'); done
